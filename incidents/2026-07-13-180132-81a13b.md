---
id: INC-20260713-180132-81a13b
timestamp: 2026-07-13T18:01:32Z
category: process
type: uncoordinated
severity: high
status: open
agent: owl
title: Обход GUARDIAN — коммит в main (4a2da7c, streikbrecher)
---

# Обход GUARDIAN — коммит в main (4a2da7c, streikbrecher)

## Описание
При аудите git-гигиены обнаружено: коммит `4a2da7c` («infra: aion_pool.py — shared Aion 5-key pool, persistent daily-exhaustion tracking») авторства **Штрейкбрехер** (`streikbrecher@labdoctorm.ru`) находится на `origin/main` репозитория `DoctorM_and_Ai`. Pre-commit хук **GUARDIAN** (правило 1) в настоящее время жёстко блокирует любые коммиты в `main` («❌ Коммиты в main ЗАПРЕЩЕНЫ»). Присутствие этого коммита на `main` означает, что хук либо был обойдён (`--no-verify`), либо на момент коммита не защищал ветку `main`.

## Обнаружение
В рамках миссии по зачистке «тихого WIP» (Вариант А). Саб-агент, действовавший от лица Муравья, попытался закоммитить `bin/aion_pool.py` в `DoctorM_and_Ai/main`, обнаружил, что файл **уже** на `origin/main` (`4a2da7c`) автором `streikbrecher`, и что GUARDIAN блокирует `main`. Подтверждено:
- `git merge-base --is-ancestor 4a2da7c origin/main` → YES
- автор коммита: Штрейкбрехер `<streikbrecher@labdoctorm.ru>`

## Решение
Требует решения ЗавЛаба / Штрейкбрехера:
1. Как `aion_pool.py` попал в `main` — проверить, не использован ли `--no-verify` на protected-ветке.
2. Нужен ли rebase/откат коммита из `main` на feature-ветку (каноничный путь), либо легализация (если коммит намеренный и согласован).
3. Убедиться, что GUARDIAN защищает `main` во ВСЕХ репозиториях (аналогичная ситуация возможна в других проектах).

## Уроки
<заполняется при закрытии>
