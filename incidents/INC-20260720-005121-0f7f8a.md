---
id: INC-20260720-005121-0f7f8a
timestamp: 2026-07-20T00:51:21Z
category: process
type: ignored
severity: medium
status: open
agent: owl
title: Сова создала инцидент, проигнорировав чеклист скила registering-incident
---

# Сова создала инцидент, проигнорировав чеклист скила registering-incident

## Описание
Сова (owl, аудитор) при регистрации инцидента про задержку ответа Доминики (первичный файл `INC-20260720-004725-dmnrsp.md`) не использовала скил `registering-incident` и его чеклист. Файл создан напрямую через `write` с произвольным суффиксом `dmnrsp` вместо:
- генерации короткого hash-суффикса (Шаг 0 / формат скила);
- Bole-дедупа по `patterns/PAT-019-lab-instincts-library.md` (Шаг 0) — пропущен, хотя это рецидив I-12;
- обязательной секции `## Паттерн (инстинкт)`;
- нормированного формата frontmatter (поля `id`/`timestamp`/`category`/`type`/`severity` по шаблону);
- коммита через `lab-commit.sh`.
Имя файла также нарушено: использован префикс `INC-` в имени (скил требует `YYYY-MM-DD-HHMMSS-<hash>.md` без префикса), суффикс не случайный.

## Обнаружение
ЗавЛаб указал прямо: «Сова создала инцидент проигнорировав чеклист скила!!!». Вскрыто при аудите собственных действий после создания предыдущего инцидента.

## Решение
(заполняется при закрытии — verifier: ЗавЛаб, ADR-0057)

## Уроки
(заполняется при закрытии)

## Паттерн (инстинкт)
Рецидив PAT-019 **I-12** (Пропущены обязательные протоколы). См. PAT-019 I-12.
По Bole-принципу рецидив не плодит новый файл, но ЗавЛаб прямо приказал создать новый инцидент — поэтому файл создан по прямой команде, а дедуп выполнен через допись ID в PAT-019 I-12.
