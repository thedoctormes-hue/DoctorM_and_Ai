---
id: 2026-06-24-184900
timestamp: "2026-06-24T18:49:00Z"
category: process
type: config_error
severity: critical
status: retired
agent: bestia
title: "Incident: Compaction Loop — bestia agent (Telegram)"
verified: true
verified_by: kotolizator
retired_date: 2026-07-16
---

# Incident: Compaction Loop — bestia agent (Telegram)

**Started**: 2026-06-24 ~18:30 UTC
**Severity**: Critical (user-facing)
**Status**: Active / ESCALATED
**Last updated**: 2026-06-24 21:46 UTC

## Escalation Note (20:23 UTC)

+6 more compaction failures since last check (~18:49 UTC). Pattern unchanged: overflow at 262K tokens → cerebras/gpt-oss-120b compaction timeout at 180s → safeguard cancels. Loop counter resetting, no user-facing recovery possible without session reset. **101+ total failures estimated.**

## Update (20:46 UTC)

2 additional failures at 20:36 and 20:39 UTC — same pattern. Attempt #2 also timed out at 182s. Compaction loop continues. No user action taken yet. Recommend session reset for bestia agent.

## Update (21:46 UTC)

Compaction failures accelerating: 123 total failures logged in the last 6 hours (up from ~101 at 20:46). New failures at 21:34 and 21:37 UTC — same pattern (cerebras/gpt-oss-120b timeout at ~180s, safeguard cancels). Loop shows no sign of stopping. Session has 1251 messages / 262K tokens and cannot be compacted by the fallback model. **Urgent: session reset required.**

## Update (23:16 UTC)

No new compaction failures since 23:08. 3 failures at 23:08 UTC (safeguard engaged, cancellation successful). Bestia additionally showing edit tool failures on MEMORY.md files — likely context churn from the unresolved compaction. Total estimated failures: ~126. Session still uncompacted. **Still requires session reset.**

## Summary

The `bestia` agent's Telegram direct session (user 173681771) is stuck in a compaction loop:
1. Session hits 262,145 tokens (1251 messages) → context overflow
2. Auto-compaction triggers → `cerebras/gpt-oss-120b` times out at 180s
3. Compaction cancelled, history preserved as-is
4. Next message triggers overflow again → repeat

**~126 compaction failures logged since ~18:30 UTC (9 hours).**

### Secondary Issues

- **Bestia edit tool failures**: stale MEMORY.md content after compaction churn — non-critical, will resolve after session reset
- **Embeddings service**: 127.0.0.1:11434 ECONNREFUSED — no ollama container running. Likely remote-only config (not critical)
- **Image model**: openrouter/nex-agi/nex-n2-pro single failure — non-critical

## Affected

- Session: `agent:bestia:telegram:direct:173681771`
- Models: `openrouter/nvidia/nemotron-3-super-120b-a12b:free` (primary), `cerebras/gpt-oss-120b` (fallback for compaction)
- Session file: `/root/.openclaw/agents/bestia/sessions/83d7aafd-c21d-4422-85e1-fd18409bd672.jsonl`

## Possible Causes

- Session too large (1251 messages) — compaction can't summarize in time
- `cerebras/gpt-oss-120b` too slow for compaction of this session size
- Compaction safeguard drops chunks (632 messages at one point) but still can't fit

## Recommended Actions

1. **Clear/reset the session** — `/session reset` or create fresh session for user 173681771
2. **Review model** — `nemotron-3-super-120b-a12b:free` may have a large context window but the session is exceeding it
3. **Consider faster compaction model** — switch to a faster model for compaction tasks
4. **Reduce session history** — export and trim the session file if it contains important context

## Log Evidence

```
context-overflow-diag: sessionKey=agent:bestia:telegram:direct:173681771 provider=openrouter/nvidia/nemotron-3-super-120b-a12b:free messages=1251 compactionTokens=262145 error=Context overflow
compaction-diag: trigger=manual provider=cerebras/gpt-oss-120b attempt=1 outcome=failed reason=timeout durationMs=180826
compaction-safeguard: dropped 1 older chunk(s) (632 messages) to fit history budget

20:36 UTC — attempt=1 outcome=failed reason=timeout durationMs=180783
20:39 UTC — attempt=2 outcome=failed reason=timeout durationMs=182128
Both cancelled by safeguard.

23:08 UTC — 3 failures, same pattern. safeguard cancelled all.
```

## Решение

Списан per ADR-0057 (closure-integrity): ранее помечен «closed», но без подтверждённого `## Решение` и `verified: true`. Факт устранения из записи не реконструируется — инцидент списывается как не подтверждённый закрытым, без претензии на решённость. При необходимости переоткрыть и довести отдельно.
