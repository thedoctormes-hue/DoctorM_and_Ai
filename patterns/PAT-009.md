---
id: PAT-009
type: pattern
title: API Key Management - Vault and Environment
status: deprecated
author: ЗавЛаб
created: '2026-06-17'
updated: '2026-06-17'
confidence: high
source: manual
last_verified: '2026-06-17'
tags: []
freshness_score: 98
last_checked: '2026-06-20T01:00:33+00:00'
---


# PAT-009: API Key Management - Vault and Environment
## Consolidated Insights

1. Понял, ЗавЛаб. Ты хочешь использовать подход **Zero Token** для себя — логиниться в свой браузер на свои аккаунты (DeepSeek, Claude, ChatGPT и т.д.) и через этот форк OpenClaw обращаться к моделям без API-ключей. Это совсем другое дело — не воровство, а использование собственных сессий.
    
    Давай разберу, что реально можно извлечь из этого проекта для нашей лаборатории.
    
    **Что полезного можно взять**...

2. **Краткий аудит текущей конфигурации OpenClaw**  
    
    ---  
    
    ### 1️⃣ Основные параметры `openclaw.json`
    
    | Раздел | Что важно |
    |---|---|
    | **gateway** | Работает в режиме `local`, слушает порт 18789, токен авторизации (`313265…1854`). Тейлскейл отключён. |
    | **agents** | 9 агентов (mangust, dominika, kotolizатор, antcat, bestia, owl, raven, streikbrecher, main). Каждый агент имеет собственный `works...

3. **Готовые шаблоны prompt‑ов для OpenClaw‑агентов**  
    (выбор сделан из официального Runbook MoltFounders, репозитория mitsuhiko/agent‑prompts и сообщества Awesome OpenClaw Templates). Каждый пункт — полное системное сообщение, которое можно сохранить в `workspace/agents/*.md` и подключить к конфигурации `openclaw.json`.
    
    ---
    
    ### 1️⃣ Monitor Agent – лёгкие проверки и оповещения  
    **Назначение:** бы...

4. **Золотые промпты для лаборатории (первичный набор)**  
    *Сформированы из официального Runbook MoltFounders, репозитория mitsuhiko/agent‑prompts и сообщества Awesome OpenClaw Templates. Все промпты можно сохранить в `~/.openclaw/workspace/agents/` как *.md и подключить к `openclaw.json`.  
    
    ---
    
    ### 1️⃣ Monitor Agent – лёгкие проверки и оповещения  
    **Назначение:** быстрый, дешёвый “heartbeat”, про...

5. Извини, я добавил информацию о моделях, хотя ты её не запрашивал. Ниже — чистый набор «золотых» промптов для типовых задач лаборатории. Каждый промпт можно сохранить в `workspace/agents/<имя>.md` и подключить к `openclaw.json` без указания моделей (они будут подбираться автоматически или задаваться отдельно).
    
    ---
    
    ### Monitor Agent  
    ```
    You are a lightweight monitoring agent for the laboratory i...

6. **Диагностика выполнена. Вот что выяснилось:**
    
    ---
    
    ### 1. **Да, всё через OpenRouter**
    - Твой `openclaw.json` содержит кастомный провайдер `arcee`, который маппит на `https://openrouter.ai/api/v1`
    - Модели Arcee (включая Trinity Mini 26B) доступны именно через OpenRouter API
    - Ключ OpenRouter найден в переменной окружения: `sk-or-…4257`
    
    ---
    
    ### 2. **Все модели OpenRouter (итого: 337)**
    - Получ...

7. Проверила всех. 🦊 Вот честная картина — и да, есть пара вещей, которые надо притормозить, чтобы не было хаоса.
    
    ## 📊 Статус всех 8 агентов (live)
    
    - 🐱 **Кот** — ✅ done. Принял обе задачи, ждёт ОК Совы, готов мерджить. Координатор на месте.
    - 🦉 **Сова** — 🟡 работает. Уже копает аудит и **нашла важное**: в `context-api.service` ключ `MYRMEX_API_KEY` вообще не пробрасывается (ни `Environment=`, ни `E...

8. Картинка ясна. Вот сводка для Доминики:
    
    ---
    
    **🦊 Доклад Ворона — разведка деплоя фикса MYRMEX_API_KEY**
    
    **✅ Пункт 2 — /etc/context-api.env:** ключ `MYRMEX_API_KEY=*** на месте.
    
    **✅ Пункт 3 — EnvironmentFile в context-api.service:** строка `EnvironmentFile=/etc/context-api.env` присутствует.
    
    **⚠️ Пункт 1 — Myrmex source:** директория `/root/LabDoctorM/services/myrmex-control/src/` **пуста** (ил...

9. Стоп — рельсы перекрыты на уровне конфига. 🦊 Вот точный след:
    
    Связь технически есть, но gateway её **глушит настройкой**:
    ```
    tools.sessions.visibility = tree
    ```
    В режиме `tree` я вижу и могу писать только в свою ветку. Чтобы лиса достучалась до Мангуста (он в `agent:mangust:main` — то есть он **существует**, рельса до него ведёт!), нужно переключить:
    ```
    tools.sessions.visibility = all
    ```
    
    Так...

10. 🦊 Разнюхала. Вот что притащила из обеих нор, ЗавЛаб:
    
    **Общий конфиг (`openclaw.json`)**
    - Дефолтная модель колонии: `openrouter/owl-alpha`, с фолбэками на бесплатные (Nemotron 3 Super 120B, Nex N2 Pro, GPT OSS 120B).
    - Gateway: локальный, loopback, порт `18789`, Tailscale выключен.
    - Память/эмбеддинги: ollama `nomic-embed-text` на `127.0.0.1:11434`.
    - Компакция: флаш памяти через `arcee/trinity-m...

11. Оба логина — `HTTP 401`, `Yandex-Uid: 0` (т.е. аутентификация не прошла, юзер не опознан). `WWW-Authenticate: Basic realm="Yandex.Disk"` — сервис отвечает, но связку логин/пароль отвергает.
    
    По нашей же заметке из памяти: `AUTHENTICATIONFAILED` у Яндекса = либо неверный пароль приложения, либо сам сервис/протокол выключен в настройках ящика.
    
    Что это значит и что нужно проверить:
    
    1. **Пароль «Кле...

12. Unit включён (enabled), но нет ни cron, ни timer. Бот задуман как разовый скрипт (проверил → опубликовал → вышел), но unit настроен на бесконечный рестарт. Это явная ошибка конфигурации.
    
    ЗавЛаб, нужна твоя команда — этот бот должен запускаться по расписанию (cron/timer), а не бесконечно рестартать. Как часто он должен работать? Раз в день? Раз в час?
    
    Пока я вижу три проблемы:
    
    **Итого по диагнос...

13. Вижу. Ключи в `.env.api-keys` замаскированы (`***`). Сова реально теряет секреты — записывает в файл, но при следующем запуске не может их прочитать или не использует.
    
    ЗавЛаб, вот что я поняла:
    
    **Что сделано:**
    - Mistral — 37/38 моделей протестировано ✅
    - Cohere — 9/11 ✅
    - Groq — 17/17 ✅
    - Cerebras — 1/2 ✅
    - Cloudflare — 9/21 ✅
    - Manus — 403 на всех, ключ не работает
    
    **Проблема Manus:** ключ да...

14. [[reply_to_current]]
    
    Сессия закрыта. Отчёт по итогам подстраховки:
    
    **Что было сделано:**
    1. ✅ Проверил — правки Муравья на месте (auth: apikey в context-index.ts и context-profile.ts)
    2. ✅ Проверил — `MYRMEX_API_KEY` совпадает в `/etc/context-api.env` и `myrmex-control/.env`
    3. ✅ Исправил корневую причину 401: в systemd unit `context-api.service` не было `EnvironmentFile=/etc/context-api.env` —...

15. Сделано. Qwen погашен чисто, OpenClaw не задет.
    
    **Что выключено:**
    - `ant-cat.service` — stop + disable (был active/enabled)
    - `qwen-channels.service` — stop + disable (был active/enabled)
    - `watch-qwen-serve.timer` — disable
    - `bestia.service` — disable (qwen `start_bestia.sh`)
    - Все 8 qwen-процессов сняты (висели с 12 июня) → **0 процессов qwen**
    
    **Контрольная проверка:**
    - 8 OpenClaw-агентов...

16. Уже проверил всё предыдущим ходом. Краткий ответ:
    
    **Ключ:** ✅ совпадает (`b5c4b9…0eca` в обоих файлах).
    **auth:** ✅ `context-index.ts` и `context-profile.ts` — `auth: 'apikey'`.
    
    **НО:** context-api отдаёт **503 "Myrmex unavailable"**. Причина — context-api (Python) стучится в myrmex-control:3000 с ключом, но получает 401. Нужно разобраться, принимает ли myrmex-control ключ в нужном формате. Эт...

17. Вижу что `generate_article` уже имеет fallback на HuggingFace. Тогда проблема в том что monkeypatch меняет `hype_orq.OPENROUTER_API_KEY` но не меняет `OPENROUTER_API_KEY` внутри функции `generate_article` (она использует глобальную переменную модуля). На самом деле monkeypatch должен работать — он заменяет атрибут модуля. Давай посмотрю что реально происходит:

18. Вот оно. Сервис работает, но:
    
    1. **В сервисе НЕ загружен MYRMEX_API_KEY** — в systemd unit нет `EnvironmentFile=/etc/context-api.env`
    2. **Лог подтверждает 401** — `Myrmex proxy upstream 401 for /api/v1/context-index: Myrmex auth failed (X-API-Key invalid or missing)`
    
    Ключи в `.env` файлах совпадают, но сервис не видит ключ. Проверю Myrmex тоже.

19. Теперь полная картина. Context-api (Python) стучится в myrmex-control на порт 3000 и получает 401 — потому что myrmex-control уже обновлён на `apikey` авторизацию, а context-api ещё не отправляет ключ. Нужно найти Python-исходники context-api и добавить заголовок с ключом.

20. Нашёл проблему. Context API проксирует запросы к Myrmex (`/api/v1/context-index`), и Myrmex возвращает 401 с сообщением "X-API-Key invalid or missing". Ключ в env есть, но Myrmex его не принимает. Нужно проверить формат ключа на стороне Myrmex.

21. Критично: в `context-api.service` НЕТ проброса `MYRMEX_API_KEY` (ни `Environment=`, ни `EnvironmentFile=`). Прокси-код его читает, но systemd его не подаёт → пункт 2 в реальности не закрыт. Проверю ключ и порт на стороне Myrmex.


