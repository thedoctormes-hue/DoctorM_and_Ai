---
id: PAT-010
type: pattern
title: OpenClaw Configuration Management
status: active
author: ЗавЛаб
created: '2026-06-17'
updated: '2026-06-17'
confidence: high
source: manual
last_verified: '2026-06-17'
tags: []
freshness_score: 98
last_checked: '2026-06-20T01:00:33+00:00'
---


# PAT-010: OpenClaw Configuration Management
## Consolidated Insights

1. We have only main and mangust .sqlite now (others removed). Need to rebuild indices for all agents, including those without .sqlite? Actually each agent's memory uses its own .sqlite in ~/.openclaw/memory/. The missing ones may be recreated upon indexing.
    
    We'll run forced reindex for all agents (no --agent) after ensuring config fixed. Config invalid error resolved earlier? Doctor fixed? Let's ve...

2. Вот сводка:
    
    **Общий статус Gateway:**
    - OpenClaw 2026.6.6, Linux 5.15.0-181-generic, Node 24.16.0
    - Gateway работает (pid 94556, systemd, active)
    - Telegram — ON, SETUP
    - **9 агентов**, 12 активных сессий
    - Модель по умолчанию: `openrouter/owl-alpha`
    - 0 активных задач, 5 issues в трекере, 14 tracked
    
    **Конфиг** (`/root/.openclaw/openclaw.json`, 20763 bytes):
    - Model: `openrouter/owl-alpha` с 5 ф...

3. Сводка по конфигурации и логам:
    
    **Конфиг OpenClaw** (`/root/.openclaw/openclaw.json`):
    
    - **9 агентов**: main, mangust, dominika, kotolizator, antcat, bestia, owl, raven, streikbrecher
    - **Модель по умолчанию**: `openrouter/owl-alpha` (1049k ctx)
    - **Фоллбэки**: Nemotron 550b/120b, GPT-OSS 120b/20b, Gemma 4 31b — все free-tier через OpenRouter
    - **Compaction**: Trinity Mini 26B для memory flush
    -...

4. 🐾 **Итоговый анализ таймаутов и пустых ответов (логи за 15‑16 июня)**  
    
    ---
    
    ### 1. Что изменилось с таймаутом
    - **agents.defaults.timeoutSeconds** был поднят с 120 → 300 (конфиг в `openclaw.json`, строка 23).  
    - После перезапуска gateway (~06:28–06:36 UTC 16‑го) все новые `embedded run timeout` фиксируются уже с `timeoutMs=300000`.  
    - Последний таймаут на 120000 мс — в 06:27:37, затем только 3...

5. Теперь у меня полная картина. Нужно:
    
    1. Добавить fallback-модели в `openrouter` провайдера
    2. Обновить `agents.defaults.model` с `primary` + `fallbacks`
    3. Добавить `timeoutSeconds` для ограничения долгих таймаутов


