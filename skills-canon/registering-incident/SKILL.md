---
name: registering-incident
description: "Регистрация инцидентов: баги, хаос, фантазии, игнор. Создаёт файл в каноническом реестре `/root/LabDoctorM/projects/DoctorM_and_Ai/incidents/`. Используй при обнаружении проблем безопасности, сбоев, нарушений процессов или несогласованных действий."
version: "1.3.0"
author: "ЗавЛаб"
last_reviewed: "2026-07-17"
status: active
metadata: { "openclaw": { "emoji": "🚨" } }
user-invocable: true
requires:
  binaries: [git]
  config: []
triggers:
  phrases:
    - "создать инцидент"
    - "завести инцидент"
    - "инцидент"
    - "баг"
    - "ошибка"
    - "сломалось"
    - "косяк"
    - "хаос"
    - "мусор"
    - "фантазии"
    - "игнор"
    - "не согласовано"
  patterns:
    - "что-то сломалось"
    - "обнаружена проблема"
    - "нужно зафиксировать"
    - "это инцидент"
  scope:
    - любая проблема в работе агентов
    - баги, ошибки конфигурации, сломанные деплои
    - хаос, мусор, таблицы в чате
    - фантазии вместо фактчекинга
    - несогласованные действия
    - игнорирование вопросов или поставленной задачи
---

# Incident Protocol

Обязательный протокол регистрации инцидентов в лаборатории.
Любой агент может создать инцидент при обнаружении проблемы.

## Когда создавать инцидент

**Технические проблемы:**
- Баг в коде
- Ошибка конфигурации
- Упавший деплой
- Потеря данных
- Уязвимость безопасности

**Процессные проблемы:**
- Хаос, мусор, таблицы в чате
- Фантазии вместо фактчекинга
- Несогласованные действия агентов
- Игнорирование вопросов
- Игнорирование поставленной задачи

## Шаг 0 — Bole-дедуп (ОБЯЗАТЕЛЬНО перед регистрацией)

> **Инсайт ЗавЛаба (2026-07-17):** инцидент — не лог ошибки, а **инстинкт** (root cause + signal),
> по которому агент узнаёт повтор и не допускает его. Без выжатого паттерна инцидент бесполезен.

**Перед созданием нового файла инцидента:**
1. Открой библиотеку инстинктов `patterns/PAT-019-lab-instincts-library.md` и найди совпадение
   по **root cause** (не по внешнему симптому). Ищи семантически (`memory-gateway__search_memory`),
   не только grep.
2. **Если инстинкт уже есть (рецидив):** НЕ создавай новый событийный файл. Допиши ID нового случая
   в строку `**ID:**` соответствующего инстинкта в PAT-019 и, при необходимости, обнови
   существующий инцидент-эталон. Рецидивы сливаются в ОДИН паттерн (Bole-принцип), а не плодятся.
3. **Если инстинкта нет (новый класс):** создавай инцидент по формату ниже и ОБЯЗАТЕЛЬНО заполни
   секцию `## Паттерн (инстинкт)` (root cause абстрактный + signal + превенция). После закрытия —
   заведи новый инстинкт `I-NN` в PAT-019.

**Правило:** инцидент без секции `## Паттерн (инстинкт)` считается неполным (это лог, а не инстинкт).

## Классификация

### Категория (category)
- `tech` — техническая проблема
- `process` — процессная проблема

### Тип (type)

**Технические:**
- `bug` — код работает не так
- `config_error` — сломанная конфигурация
- `deploy_failure` — деплой не прошёл
- `data_loss` — потеря данных
- `security` — уязвимость

**Процессные:**
- `chaos` — хаос, мусор, таблицы в чате
- `fantasy` — фантазии вместо фактчекинга
- `uncoordinated` — несогласованные действия
- `ignored` — игнорирование вопросов или задач

### Серьёзность (severity)
- `critical` — блокирует работу, немедленная эскалация ЗавЛабу
- `high` — серьёзная проблема, эскалация ЗавЛабу
- `medium` — решается в рабочем порядке
- `low` — бэлог

### Статус (status)
`open` → `investigating` → `resolved` → `closed`
> Переход `resolved → closed` — **только через verifier/ЗавЛаб** (ADR-0057). `closed` нельзя
> ставить самому автору инцидента.

## Хранилище

**Путь:** `/root/LabDoctorM/projects/DoctorM_and_Ai/incidents/YYYY-MM-DD-HHMMSS-<hash>.md`

Один файл на инцидент. Имя файла = таймстемп + короткий hash.

### Генерация уникального суффикса

Для предотвращения коллизий при создании нескольких инцидентов в одну секунду используй короткий hash (6 символов):

```bash
# Генерация hash-суффикса
SHORT_HASH=$(head -c 100 /dev/urandom | sha256sum | cut -c1-6)
FILENAME="/root/LabDoctorM/projects/DoctorM_and_Ai/incidents/$(date +%Y-%m-%d-%H%M%S)-${SHORT_HASH}.md"
```

Hash также подставляется в поле `id` и `timestamp` frontmatter:
- `id: INC-YYYYMMDD-HHMMSS-<hash>`
- `timestamp: YYYY-MM-DDTHH:MM:SSZ` (timestamp без hash — для читаемости)

## Формат файла

```markdown
---
id: INC-YYYYMMDD-HHMMSS-<hash>
timestamp: YYYY-MM-DDTHH:MM:SSZ
category: tech | process
type: bug | config_error | deploy_failure | data_loss | security | chaos | fantasy | uncoordinated | ignored
severity: critical | high | medium | low
status: open
# Поля верификации заполняет ТОЛЬКО verifier/ЗавЛаб при closure (ADR-0057):
# verified: true
# verified_by: <verifier-agent, обязан отличаться от agent>
# verified_at: YYYY-MM-DD
agent: <кто обнаружил>
title: <краткое описание>
---

# <title>

## Описание
<подробное описание проблемы>

## Обнаружение
<как обнаружено, контекст>

## Решение
<заполняется при закрытии>

## Уроки
<заполняется при закрытии, что извлечено>

## Паттерн (инстинкт)
<ОБЯЗАТЕЛЬНО для нового класса. Выжми по Bole-принципу:>
<- **Root cause (абстрактный):** первопричина без привязки к конкретному событию>
<- **Signal:** при каком симптоме агент должен вспомнить этот инстинкт>
<- **Превенция:** как не допустить повтор>
<Рецидив существующего инстинкта в PAT-019 → сюда пиши «см. PAT-019 I-NN», а ID дописывай в PAT-019.>
```

## Процесс

### 1. Создание

1. Определить category, type, severity
2. Сгенерировать короткий hash: `SHORT_HASH=$(head -c 100 /dev/urandom | sha256sum | cut -c1-6)`. Создать файл `/root/LabDoctorM/projects/DoctorM_and_Ai/incidents/YYYY-MM-DD-HHMMSS-${SHORT_HASH}.md` по шаблону
3. Подставить hash в поле `id`: `INC-YYYYMMDD-HHMMSS-<hash>`
4. Заполнить обязательные поля
5. Закоммитить через `lab-commit.sh`:
   ```bash
   /root/LabDoctorM/projects/DoctorM_and_Ai/scripts/lab-commit.sh <агент> -m "INC: <title>"
   ```

**Hash обязателен.** Без hash-суффикса два инцидента, созданных в одну секунду, приведут к коллизии имён файлов.

### 2. Эскалация

- **critical/high** → немедленно уведомить ЗавЛаба
- **medium** → решить в рабочем порядке
- **low** → записать в бэлог

### 3. Closure requires verifier (ADR-0057)

**Capture** (создание + ведение до `resolved`) — любой агент:
1. Заполнить `## Решение` и `## Уроки`.
2. Перевести `status: resolved` (решение найдено, но ещё НЕ подтверждено).
3. Закоммитить: `/root/LabDoctorM/projects/DoctorM_and_Ai/scripts/lab-commit.sh <агент> -m "INC resolved: <title>"`

**Closure** (переход `resolved → closed`) требует **второй инстанции** по severity
(автор инцидента НЕ может сам перевести в `closed` — самоподтверждение запрещено):
- `critical` / `high` → закрывает **ЗавЛаб** лично, через разбор.
- `medium` → подтверждает выделенный **verifier-агент** (вторая инстанция).
- `low` → кандидат на **auto-retire** по TTL (3 дня); при списании `status: retired`, НЕ `closed`
  (честный архив вместо фальшивого closed, ADR-0057 §D).

Closure-шаг (любой верификатор) обязан дописать в frontmatter:
- `verified: true` (булево);
- `verified_by: <verifier-agent>` — **обязан отличаться от `agent`** (self-verify запрещён);
- `verified_at: <date>`.

Итоговый коммит закрытия (от имени verifier'а, не автора):
```bash
/root/LabDoctorM/projects/DoctorM_and_Ai/scripts/lab-commit.sh <verifier-агент> -m "INC closed: <title>"
```
Гейт доказательства закрытия — ADR-0057. `incident-linter.sh --strict-closure` блокирует
коммит, если `## Решение` пуста, `verified != true` или `verified_by == agent`.

## Правила

- Коммитить ТОЛЬКО через `lab-commit.sh`
- Не коммитить `.env` и секреты
- **Закрывать `status: closed` может только вторая инстанция (verifier-агент или ЗавЛаб) — самоподтверждение автором ЗАПРЕЩЕНО (ADR-0057).**
- `low`-инциденты без решения списываются в `retired` по TTL, а не маскируются `closed`.
- Один инцидент = один файл
- Не создавать пустые инциденты

## Связанные скиллы

- `accepting-work` — протокол приёмки работы (закрытие инцидента = часть приёмки)
- семантическая память — MCP `memory-gateway__search_memory` (поиск по инцидентам/ADR)
