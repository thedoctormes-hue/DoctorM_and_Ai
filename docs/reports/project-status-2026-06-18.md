# Отчёт о состоянии проекта — 2026-06-18

## Общие сведения

- **Проект:** Система инсайтов + Myrmex Control + Context API
- **Команда:** Доминика (Scout), Сова (Auditor), Мангуст (Analyst), Муравей (Builder), КотОлизатор (Orchestrator)
- **Период:** 2026-06-18 04:57 — 10:05 UTC

---

## 1. Myrmex Control (порт 3000)

| Метрика | Значение |
|---------|----------|
| Проекты | 17 |
| Задачи | 0 |
| Артефакты | 201 |
| Агенты | 8 |
| Staff | 8 |
| Последнее обновление | 2026-06-18T10:05 UTC |

**Связь с Context API:**
- Прокси `/api/context/*` → `127.0.0.1:8100` — написан и работает
- Авторизация: `requireAuth` (token-based)
- Myrmex API требует auth (401 без токена) — Context API доступен только через прокси с авторизацией

---

## 2. Context API (порт 8100)

| Метрика | Значение |
|---------|----------|
| Версия | 1.2.0 |
| Статус | ✅ работает |
| Модель эмбеддингов | bge-m3 |
| Размер индекса | 335 артефактов |
| Последнее обновление индекса | 2026-06-18T03:59 UTC |

**Проблемы:**
- Индекс обновляется вручную (скрипт `reindex_if_changed.py`)
- Нет автобилда при изменении артефактов
- Context API v1.1.0 дублируется в `projects/myrmex-control/context-api/` (разработческая копия)

---

## 3. Система инсайтов

| Метрика | Значение |
|---------|----------|
| Исходный объём | 17K+ raw инсайтов |
| После очистки | 238 (v3) |
| Уникальных тем | 8 (остальное — session-mining шум) |
| Типы | finding:103, decision:82, error:30, pattern:21 |

**Семантический дедуп:**
- Context API готов (bge-m3, 1024-dim)
- Реализован скрипт `semantic_dedup.py`
- НЕ интегрирован в основной конвейер — запланирован на следующий этап

---

## 4. Артефакты, созданные/обновлённые сегодня

### ADR (proposed)

| ID | Тема |
|----|------|
| ADR-031 | Разделение myrmex.json на projects + agent_zones |
| ADR-032 | openclaw.json как единственный источник реестра агентов |
| ADR-033 | Два файла myrmex.json (корневой + серверный) |
| ADR-034 | Git worktree как стандарт изоляции |
| ADR-035 | Структура монорепозитория |
| ADR-036 | Миграция Qwen→OpenClaw (hooks → skills) |

### Patterns (proposed)

| ID | Тема |
|----|------|
| PAT-012 | Автоматизация проверки здоровья артефактов |
| PAT-013 | Запрет таблиц в Telegram |

### Примечание
Старые версии ADR (ADR-030..032) имеют status `deprecated` или `resolved`. Новые версии — `proposed`.

---

## 5. Текущие проблемы

### Критические
1. **47 уязвимостей безопасности** (26 high) — обнаружены Совой
2. **WireGuard приватный ключ в открытом git** — инцидент
3. **agentToAgent.enabled отсутствует** в openclaw.json — межагентная связь ограничена

### Архитектурные
1. Фронтенд Myrmex (Knowledge.tsx, ForceGraph.tsx) — изменения не в проде (не пересобран)
2. Semantic index не обновляется автоматически
3. Дублирование context-api (services/ vs projects/myrmex-control/)

### Процессные
1. Мангуст упирается в LLM idle timeout (60s) при запуске тестов
2. 108 тестов artifact-pulse — зелёные. 36 тестов `test_insights.py` — падают (SQLite миграция)
3. Graph visualization — нечитаем (227 из 238 узлов в одном кластере)

---

## 6. Дедлайны текущего спринта

| Время | Задача | Ответственный |
|-------|--------|---------------|
| 16:00 UTC | Mg3+Mg4+Mg5, M5+M6 | Мангуст + Муравей |
| 17:00 UTC | S3+S4 | Сова |
| 18:00 UTC | Итоговый дайджест | Доминика + КотОлизатор |

---

## 7. Рекомендации

1. **Увеличить LLM timeout** для Мангуста (сейчас 60s, тесты не успевают)
2. **Пересобрать фронтенд** Myrmex и задеплоить
3. **Автоматизировать билд semantic index** (cron или watcher)
4. **Закрыть критические уязвимости** безопасности
5. **Решить проблему графа** — визуализировать 8 реальных тем, а не 238 узлов

---
_КотОлизатор, 2026-06-18T10:05:00Z_
